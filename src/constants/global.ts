export const AppName = 'Budget Tracker'

export const AppDescription = `${AppName} is for tracking expenses and viewing reports on them.`

/**
 * @see https://quasar.dev/style/color-palette
 */
export const AppHeaderColor = 'primary'

/**
 * Used as part of the name of the database in the browser.
 * - Change after deployment if you want to force database to be recreated
 */
export const AppDatabaseVersion = 1
